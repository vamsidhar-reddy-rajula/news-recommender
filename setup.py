from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='A news recommending app that surfaces the hidden links and patterns which might not catch common eye',
    author='Vamsidhar Reddy Rajula',
    license='MIT',
)
